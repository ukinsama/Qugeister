# Qugeister

This repository contains an implementation of the Geister board game used for experiments.

## Setup
Install the required packages:

```bash
pip install -r requirements.txt
```

## Running tests
Execute the unit tests with:

```bash
pytest
```
